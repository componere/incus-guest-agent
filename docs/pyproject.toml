[project]
name = "incus-guest-agent-docs"
version = "0.0.0"
description = "Operator documentation for the Incus guest agent on Talos."
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.0",
]

[tool.uv]
package = false
