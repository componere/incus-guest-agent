version: v1alpha1
metadata:
  name: incus-guest-agent
  version: "{{ .VERSION }}"
  author: Componere
  description: |
    [{{ .TIER }}] Runs the Incus host-supplied guest agent in Talos virtual machines.
  compatibility:
    talos:
      version: ">= v1.13.0"
